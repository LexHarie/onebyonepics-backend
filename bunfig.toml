name = "onebyonepics-backend"

tsconfig = "tsconfig.json"

# Disable strict TLS for self-signed database certificates
[run.env]
NODE_TLS_REJECT_UNAUTHORIZED = "0"

# Preload script to set env vars before any modules
preload = ["./src/preload.ts"]
